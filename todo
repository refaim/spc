Общее: 
    статистика тестирования
    вынести работу с getopt в отдельный модуль (потому как нужна и в spc, и в тестере)
    переписать вывод (зело страшен и велик) 

ЛА: 
    double dot внутри float
    рефакторинг read_string
    возможно убрать восстановление после ошибки (надо подумать, как оно с СА сочетается)
    разбор вещей типа 'a'#32'b'

СА:
    вывод дерева: остался баг с генерацией «y» для правой ветви в случае, 
        когда правый операнд не бинарная операция]
    parse_expr() — убрать повторяющиеся куски
    если в файле синтаксическая ошибка, то последнее корректное выражение не выводится
        [не удается воспроизвести]
    баги разбора выражений: a-, (
    проблема: восстановление после лексической ошибки ломает 
        механизм определения позиции синтаксической ошибки:
        var 1as23dasd и var 213123 дают разные filepos