Общее:
    сообщения об ошибках:
        spc: file(line, col): Syntax error, <message>

    вынести работу с getopt в отдельный модуль (потому как нужна и в spc, и в тестере)
    написать информацию о тестах, выводить при фэйлах
    допилить скрипт просмотра тестов
    переехать на кортежи вместо списков, где это возможно

ЛА:
    разбор вещей типа 'a'#32'b'
    протестировать разбор операций, отличных от +-/*
    переписать вывод токенов (см. formatter и writer в языке)

СА:
    баги разбора выражений: 'a-' (и странное поведение при a()b [это из-за бага])
    скобки! [не работают из-за костылика]
    протестировать унарные операторы и бинарные, отличные от +-/*

    переехать на "a expected, but b found"
    parmismatch: выводить не позицию "(", а позицию отсутствующей ")"
    parse_expr() — убрать повторяющиеся куски
    Парсер псевдоязыка:
        исключение EOF: сделать и проверить тесты 002 014 016 018 021 022 024
